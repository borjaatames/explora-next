# Contexto Claude — ExploraSpain

Este archivo lo lee Claude al inicio de cada sesión para retomar contexto del proyecto.

## Stack
- Next.js 14.2.33 (App Router) — NO subir a Next 15
- React 18.3.1
- TypeScript estricto
- Tailwind CSS 3.4.6 + `@tailwindcss/typography`
- Fuentes: Inter + Playfair (via `next/font`)
- Markdown: `gray-matter` + `remark` + `remark-gfm` + `remark-html`
- Lightbox: `yet-another-react-lightbox`

## Arquitectura de rutas (dual)
- `app/(es-shell)/*` — rutas ES legacy con slugs en español (`/guias`, `/ciudades`, `/contacto`...)
- `app/[lang]/*` — rutas multiidioma con slugs traducidos (`/en/guides`, `/en/cities`...)
- `app/sem/*` y `app/en/sem/*` — landings SEM de afiliación con Viator (noindex)

## Idiomas
- Activos: `es`, `en`
- Configurados pero inactivos: `de`, `fr`, `it`, `pt` (diccionarios actualmente son copia del ES — RIESGO)

## Contenido
- `content/guias/{es,en}/{ciudad}/*.md` — 21 + 21 guías
- `content/ciudades/{es,en}/*.md` — 5 + 5 ciudades (Barcelona, Granada, Madrid, Salamanca, Sevilla)
- `content/actividades/{es,en}/madrid/*.md` — 18 + 20 actividades
- `content/sem/*.md` y `content/sem/en/*.md` — 2 + 2 landings SEM

## Guía de estilo
- Paleta: SOLO `sky`, `amber`, `slate`, `white`. Prohibido cualquier otro color.
- Tipografía: Playfair solo en H1-H3 editoriales; Inter en todo lo demás. Pesos: 400, 600, 700.
- Container: solo `max-w-3xl` (editorial), `max-w-5xl` (listados), `max-w-7xl` (amplio).
- Border radius: máximo `rounded-xl`.
- CTAs amber (comerciales) y sky (informativos). NUNCA mezclar.
- Focus visible obligatorio con `ring-2 ring-offset-2`.
- Documento de referencia: `docs/` en este repo (si existe).

## Documentos de referencia (LEER EN ESTE ORDEN al retomar)

1. **`C:\Users\borja\Documents\Claude\Projects\programacion de la pagina\reglas-proyecto-exploraspain.md`** — documento maestro con TODAS las reglas, decisiones preservadas, identidades, bugs documentados, excepciones y backlog priorizado. **LEER PRIMERO SIEMPRE.**
2. **`C:\Users\borja\Documents\Claude\Projects\programacion de la pagina\auditoria-exploraspain.md`** — informe ejecutivo de auditoría del 12 may 2026.

## Reglas de oro (resumen — el detalle está en reglas-proyecto-exploraspain.md)
1. NUNCA tocar archivos sin confirmar con Borja.
2. Antes de cambios estructurales, sugerir `git commit` de checkpoint.
3. Cualquier cambio que toque `.md` puede romper `prebuild` (audit-parejas) — verificar.
4. NO tocar Toledo SEM ni componentes SEM existentes (Camino 1).
5. NO subir Next.js a versión 15.
6. NO usar colores fuera de la paleta sky/amber/slate/white.
7. NO usar pesos de fuente fuera de 400/600/700.
8. Antes de proponer una mejora, comprobar si ya está en el backlog priorizado.
